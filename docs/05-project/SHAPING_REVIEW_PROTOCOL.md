# 传统蒙古文字形审核协议

每个候选金标准必须提交：原始按键、候选提交文本、输入法和版本、编码模式、字体和版本、正确截图、语义/转写、审核者及日期。

审核状态只有以下五种：

- `captured`：材料已收集；
- `machine_verified`：码位、字体哈希和 glyph ID 可复现；
- `linguist_verified`：母语／文字专家确认；
- `approved`：可以进入公开示例和覆盖规则；
- `rejected`：已证明错误，不得发布为正确示范。

机器验证不能自动升级为语言学确认。不同输入法结果不一致时全部保留，不以多数票或字体美观程度裁决。
